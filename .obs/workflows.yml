workflow:
  steps:
    - trigger_services:
        project: systemsmanagement:cobbler:github-ci:release33
        package: cobbler-docker-testing
  filters:
    event: push
    branches:
      only:
        - release33