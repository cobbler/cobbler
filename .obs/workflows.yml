workflow:
  steps:
    - trigger_services:
        project: home:cobbler-project:github-ci:release33
        package: cobbler-docker-testing
  filters:
    event: push
    branches:
      only:
        - release33