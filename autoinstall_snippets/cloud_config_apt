  apt:
    preserve_sources_list: false
    mirror-selection:
      primary:
        - arches: [default]
          uri: http://$http_server/cblr/links/$distro
#set $repo_data = $getVar("repo_data",[])
#for $repo in $repo_data
 #if $repo.mirror_locally
        - arches: [default]
          uri: http://$http_server/cblr/repo_mirror/${repo.name}
 #else
        - arches: [default]
          uri: ${repo.mirror}
 #end if
#end for
